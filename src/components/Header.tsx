const Header = () => {
    return (
        <header className="main-header">
            <h1 className="logo">MindMap</h1>
            <div className="header-actions">
                <span className="header-subtitle">Portfolio demo — data saved locally</span>
            </div>
        </header>
    );
};

export default Header;
